{{ form_start(form) }}

    {{ form_row(form.content, {
                    label: 'Content : ', 
                    'attr': {'class': 'form-control'},
                    'attr': {'style': 'height: 250px; width: 100%'}},
                    ) }}

    <br>
    <button class="btn btn-secondary">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}


